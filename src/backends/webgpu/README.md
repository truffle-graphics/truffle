# WebGPU Backend

This explicit target remains `source_only`. Its factory exposes no adapter
until a pinned local Emdawnwebgpu package is compiled and browser device
initialization succeeds. Configure does not use Emscripten's downloading remote
port.
