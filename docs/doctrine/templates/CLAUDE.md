# Claude Code Repository Adapter

Copy this file to the repository root as `CLAUDE.md` when Claude Code is used.
It intentionally contains no separate policy: the portable repository sources
remain `AGENTS.md` and `PROJECT_CONTEXT.md`.

@AGENTS.md
@PROJECT_CONTEXT.md
